{
  "name": "postureplus",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.30.3",

  "workspaces": [
    "apps/*",
    "packages/*"
  ],

  "scripts": {
    "build": "turbo run build"
  },

  "devDependencies": {
    "turbo": "^2.8.12"
  },

  "dependencies": {
    "@supabase/supabase-js": "^2.98.0"
  }
}